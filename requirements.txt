# Install PyTorch with CUDA first (pick the index for your CUDA version), e.g.:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu126
torch>=2.6
torchvision>=0.21
numpy>=1.26
matplotlib>=3.8
pytest>=8.0
